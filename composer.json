{
    "name": "wordpress/php-mcp-schema",
    "description": "Exact-revision PHP schema runtime for the Model Context Protocol (MCP)",
    "type": "library",
    "license": "GPL-2.0-or-later",
    "keywords": [
        "mcp",
        "model-context-protocol",
        "schema",
        "php"
    ],
    "homepage": "https://github.com/WordPress/php-mcp-schema",
    "authors": [
        {
            "name": "WordPress",
            "homepage": "https://wordpress.org"
        }
    ],
    "require": {
        "php": ">=7.4"
    },
    "require-dev": {
        "phpstan/phpstan": "^1.10",
        "phpunit/phpunit": "^9.6"
    },
    "autoload": {
        "psr-4": {
            "WP\\McpSchema\\": "src/"
        }
    },
    "config": {
        "platform": {
            "php": "7.4.0"
        },
        "sort-packages": true
    },
    "scripts": {
        "analyse": "phpstan analyse",
        "artifact:verify": "tests/artifact/verify.sh",
        "autoload:verify": "@composer dump-autoload --optimize --strict-psr --strict-ambiguous",
        "phpstan": "@analyse",
        "test": "phpunit"
    }
}

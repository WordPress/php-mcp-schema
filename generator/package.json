{
  "name": "@wordpress/php-mcp-schema-generator",
  "version": "2.0.0",
  "description": "Generate exact-revision PHP catalogs and shared records from pinned MCP JSON Schemas",
  "private": true,
  "type": "module",
  "scripts": {
    "audit": "node audit-schemas.mjs",
    "audit:check": "node audit-schemas.mjs --check",
    "generate": "node generate.mjs",
    "generate:check": "node generate.mjs && node check-generated.mjs",
    "lint": "node --check audit-schemas.mjs && node --check check-generated.mjs && node --check generate.mjs && node --check update-schemas.mjs && node --check lib/php-code.mjs && node --check lib/schema-tools.mjs",
    "schemas:check": "node update-schemas.mjs --check",
    "schemas:update": "node update-schemas.mjs",
    "test": "node --test test/*.test.mjs",
    "verify": "npm run lint && npm test && npm run schemas:check && npm run audit:check && npm run generate:check"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "ajv": "8.17.1"
  }
}
